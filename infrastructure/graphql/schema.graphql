type recipeURL @aws_cognito_user_pools {
  id: ID!
  url: String!
  owner: String!
}

type Mutation {
  createRecipeURL(inputUrl: String!): recipeURL!
    @aws_cognito_user_pools
}

type Query {
  listRecipeURLs: [recipeURL]!
    @aws_cognito_user_pools
}